<div class="container">
  <div class="row justify-content-center">
    <div class="col-xs-12 col-sm-6 col-md-4">
      <div class="form-login">
        <h2>Log in</h2>
        <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
          <div class="form-inputs">
            <%= f.input :email, required: false, autofocus: true, placeholder: "seu-email@mail.com" %>
            <%= f.input :password, required: false, placeholder: "••••••", label: 'Senha' %>
            <%= f.input :remember_me, label: 'Lembrar de mim', as: :boolean if devise_mapping.rememberable? %>
          </div>
          <div class="form-actions">
            <%= f.button :submit, "Log in", class: "btn btn-primary" %>
          </div>
          <%= render "devise/shared/links"%>
          <%= render "devise/shared/gbutton" %>
        <% end %>
      </div>
    </div>
  </div>
</div>





