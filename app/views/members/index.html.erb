<div class="container vaccine-container">
  <div class="row">
    <div class="container">
      <div class="row">
        <% @members.each do |member| %>
          <% if vaccine_expired(member).size != 0 %>
            <div class="col-12 col-sm-6 text-center py-2 flash-pos custom-margin-top">
              <div class="flash flash-danger alert-dismissible fade show animated slow     bounce delay-5s alert-x" role="alert">
                <span><strong><%= link_to "#{member.first_name}: Está com alguma vacina atrasada! 😱", member_path(member), class: "alert-link" %></strong></span>
                <a data-dismiss="alert" aria-label="Close">
                </a>
              </div>
            </div>
          <% end %>
        <% end %>
      </div>
    </div>
  </div>

  <br>

  <div class="row d-flex justify-content-between">
    <div class="col-12 col-lg-4">
      <div class="card mb-2">
        <div class="card-body">
          <h5 class="card-title text-center">CADASTRE SEUS FAMILIARES</h5>
          <p class="card-text">Cadastre você como titular da conta e seus familiares como dependentes.</p>
          <a href="#" class="btn btn-primary">Go somewhere</a>
        </div>
      </div>
    </div>

    <div class="col-12 col-lg-4">
      <div class="card mb-2">
        <div class="card-body">
          <h5 class="card-title text-center">VACINAS ATUALIZADAS</h5>
          <p class="card-text">Preencha os dados de vacinas passadas no perfil do familiar e mantenha o histórico atualizado</p>
          <a href="#" class="btn btn-primary">Go somewhere</a>
        </div>
      </div>
    </div>

    <div class="col-12 col-lg-4">
      <div class="card mb-2">
        <div class="card-body">
          <h5 class="card-title text-center">AGENDE NOVAS VACINAS</h5>
          <p class="card-text">Agende e pague por vacinas pendentes em clínicas particulares com conveniência</p>
          <a href="#" class="btn btn-primary">Go somewhere</a>
        </div>
      </div>
    </div>
  </div>

  <h1>MEUS FAMILIARES</h1>

  <div class="member-cont my-4">
    <% @members.each do |member| %>
    <%= link_to member_path(member) do %>

    <div class="card-member m-3">
      <div class= 'card-content' >
        <% if vaccine_expired(member).size != 0 %>
        <div class="notif">
          <span class="align-middle"><h3><%= vaccine_expired(member).size %></h3></span>
        </div>
        <% end %>


        <% if member.photo.url %>
        <%= cl_image_tag member.photo, class: "rounded-circle img-fluid" %>
        <% else %>
        <%= image_tag "default.jpg", class: "rounded-circle img-fluid"  %>
        <% end %>
        <h3 class="mt-3"> <b> <%= member.first_name %></b> </h3>

      </div>
    </div>
    <% end %>
    <% end %>
  </div>
</div>
